{
  "name": "lobos-trucking-firebase-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-lobos-trucking --config ../flutter_app/firebase.json --only firestore,storage \"node --test firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.16.0",
    "firebase-tools": "15.24.0"
  }
}
