{
  "name": "lobos-firestore-checks",
  "private": true,
  "scripts": {
    "test:rules": "firebase emulators:exec --only firestore --project demo-lobos \"node --test test/firestore_rules.test.cjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-admin": "^14.4.0",
    "firebase-tools": "^15.0.0"
  }
}
